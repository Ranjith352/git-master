{
  "name": "ci-demo",
  "version": "1.0.0",
  "scripts": {
    "test": "npx mocha test/*.js --reporter mocha-junit-reporter --reporter-options mochaFile=test-results.xml"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "mocha-junit-reporter": "^2.2.0"
  }
}
